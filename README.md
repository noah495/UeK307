# BetterTiq

To start frontend, run 
```
    npm run install:all
    npm run start:fe
```

To start backend, run
```
    npm run install:all
    npm run start:be
```

To run e2e tests, run
```
    npm run cy:run
```
or to run e2e tests in cypress UI, run
```
    npm run cy:open
```
